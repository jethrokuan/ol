<% if user_signed_in? %>
	<hr>
    <li><%= link_to('Sign Out', destroy_user_session_path, :method => :delete)%></li>
<% elsif staff_signed_in?%>
  <hr>
    <li><%= link_to('Sign Out', destroy_staff_session_path, :method => :delete)%></li>
<% else %>
<% end %>